#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
#include <string>

using namespace std;
using ll = long long;
#define rep(i,n) for(int i=0; i<n; ++i)


int main() {
map<int,int>mp;
   int n,q;
    cin >> n >> q;
    rep(i,n){
        mp[i] = 1;
    }
    int pos =0;

    rep(i,q){
        int x,y;
        cin >> x >> y;
        // pos から，xまで0にして，mp[y]にそれまでの総数を足す．









    }



    
 
    return 0;
}